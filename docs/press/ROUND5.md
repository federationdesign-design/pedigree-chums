# Press pack revisions, round 5

**Owner notes, 30 August 2026. Build exactly as written.**

Do not edit, improve, substitute or question any request below. Repeated images are intentional.

This round inserts a new screen after 3, so the numbering after that point shifts by one. Report the resulting order once built.

---

## Screen 3

- Update the copy to:

> There may be millions of Pug outside the cards. but in the chums world, there is only Pug. Every real Pug you see is the Pug.

- Add a row of five images below the two columns: `alt-pug1.jpg`, `alt-pug2.jpg`, `alt-pug3.jpg`, `alt-pug4.jpg`, `alt-pug5.jpg`. All in `~/Downloads`.

---

## New screen, inserted after 3 and before 4

- Keep the title "Meet Pug".
- Replace the current four-image grid with the video `pug-run.mp4`.

---

## Screen 6

- Add a second column.
- Add a second video, `portrait-advert.mp4`.

---

## Screen 7

- Add a second item in column B: the image `blue-orig1.jpg`.
- Organise the layout so it is constrained in a neat grid, like the previous screens.

---

## Notes for the build

**The neat grid rule now applies to screens 6, 7 and 8.** All three are two-column screens carrying a mix of images and video. Every one should read as a single rectangle with the columns equal in height, edges aligned top and bottom, and no gaps around the assets. Fix the height mismatch at its cause rather than per screen.

**Build everything above exactly as written.** Report anything that will not work only after building the rest.
