# Press pack revisions: screens 1 to 7

**Owner notes, 29 August 2026. Apply in order.**

Note this is a RENUMBERING as well as a set of changes. A screen is inserted, so what follows shifts. The owner's screen 6 is explicitly "was 5", which confirms it.

Four items are ambiguous and must be raised rather than guessed. They are marked **ASK**.

---

## Screen 1

- Make the image twice as big.
- Overlay the text container over the image, at the bottom, by 15px.
- **ASK:** "add toggle for text colour change". The site already has the A/A/A contrast toolbar and the text-invert toggle used on the home page. Report whether the owner means that existing pattern, a new per-screen control, or something else, before building anything.
- Change the image to video. `ad1d.mp4` is now on Vimeo at `https://vimeo.com/1222451619`. Use the same click-to-play facade as the other clips.

---

## Screen 2

- Keep the image the same size, but put a second image beside it so two appear on screen.
- Use `card-on-cartoon.jpg` then `card-on-real.jpg`.
- Overlay the text container over the image, at the bottom, by 15px.
- Replace the copy with this, verbatim:

> **The Card Is the Lens**
>
> Every Chum begins in the card, where illustration turns a breed into something you can imagine, recognise and play with, helping you notice, recognise and understand the dogs that were already around you.
>
> The card introduces the dog.
> Your imagination gives the dog character.
> The real world brings the dog to life.
>
> The world has not changed. The way you look at it has.

The three middle lines are a list. The owner's source mixed bullet markers, so set them consistently.

---

## Screen 3

- Keep the image the same size, add another beside it.
- **ASK:** the image list reads "card-on-real.jpg then card-on-real.jpg and dog-on-real.jpg". That is `card-on-real` twice, and three files against an instruction for two. Report and confirm before wiring.
- Replace the copy with this, verbatim:

> **Meet Pug**
>
> One of the 54 Chums.
>
> There may be millions of dogs outside the cards. In our world, there is only one Pug, Ie its all the same Pug. Pug had instincts. Pug simply acts and behaves like any other dog does.

Leave the wording exactly as given, including "Ie its", unless the owner corrects it. Flag it as likely intended to read "i.e. it is all the same Pug".

---

## Screen 4

- Lay the images out as a 2 by 2 grid.
- Use `slide13.jpg`, `slide14.jpg`, `slide14b.jpg`, `dog-on-real.jpg`.

---

## Screen 5

- Same 2 by 2 grid treatment as screen 4.
- Use `dog-on-real.jpg`, `slide15.jpg`, `slide16.jpg`, `cover.jpg`.
- **ASK:** the source listed `slide16.jpg.jpg` with a double extension, read here as `slide16.jpg`. Confirm. Also note `dog-on-real.jpg` now appears on screens 3, 4 and 5.

---

## Screen 6

- This was screen 5. Keep the video.
- Use a custom poster rather than the standard Vimeo one: frame 1 of the video, extracted with ffmpeg, as was done for the other facades.

---

## Screen 7

No changes.

---

## Notes for the build

**Screens 8 onward are untouched for now.** The owner will refine them next.

**The renumbering shifts everything after the insertion.** Report the resulting order before wiring, so the owner can see where each screen and image now lands.

**Overlaying text on images is new to this pack.** Screens 1 and 2 put the copy container over the picture rather than below it. Report how the copy reads against those images, since the pack has so far kept text on the blue-fade panel precisely so it never sits on photography.
